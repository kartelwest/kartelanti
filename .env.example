# Northstar AI environment placeholders
# No privileged API keys are required for the local-first V1 build.
# Future remote AI provider endpoint (kept server-side, never bundled to client):
# EXPO_PUBLIC_AI_PROVIDER=local
